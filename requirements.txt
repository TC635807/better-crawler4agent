# Runtime
# playwright 与浏览器 revision 强绑定：1.61 用 rev 1228，1.62 用 rev 1234。
# 不锁版本会导致"复用已有浏览器"因 revision 不匹配而启动失败。
# 升级前请确认浏览器同步重装，或设置 BETTER_CRAWLER_BROWSERS_PATH 指向匹配目录。
playwright==1.61.0
mcp>=2.0.0
trafilatura>=2.0.0
beautifulsoup4>=4.12.0
lxml>=5.3.0
httpx[http2]>=0.27.0

# Dev / Test
pytest>=8.0
